sonar.projectKey=testng-api-automation
sonar.projectName=Reqres API Automation Framework
sonar.host.url=http://localhost:9000

sonar.sources=src/main
sonar.tests=src/test
sonar.java.binaries=target/classes
sonar.java.test.binaries=target/test-classes
sonar.java.source=17

# Sonar login/token must be supplied at scan time via the SONAR_TOKEN
# env var (-Dsonar.token=$SONAR_TOKEN) or SONAR_TOKEN itself — never
# hardcoded here.
