# Staging environment configuration (-Denv=staging)
# Env-independent defaults (timeouts, ports, api.key) live in
# common.properties - only genuinely environment-specific values belong here.
# Placeholder: Reqres only exposes a single public host today. Replace
# with the real staging host(s) once available - flagged as an assumption,
# not invented infrastructure.
rest.base.url=https://reqres.in/api
